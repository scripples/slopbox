services:
  postgres:
    image: postgres:17
    ports:
      - "8322:5432"
    environment:
      POSTGRES_USER: slopbox
      POSTGRES_PASSWORD: slopbox
      POSTGRES_DB: slopbox
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
